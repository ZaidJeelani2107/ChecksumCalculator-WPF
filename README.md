# ChecksumCalculator-WPF
A lightweight WPF-based checksum calculator that supports multiple hashing algorithms, allowing users to generate and compare file checksums through an intuitive drag-and-drop interface.
